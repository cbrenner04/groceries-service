<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %></p>

<p><%= t("devise.mailer.invitation_instructions.someone_invited_you", url: Rails.configuration.client['url']) %></p>

<p><%= link_to "Accept invitation", "#{Rails.configuration.client['url']}/users/invitation/accept?invitation_token=#{@token}" %></p>

<% if @resource.invitation_due_at %>
  <p>This invitation will expire on <%= @resource.invitation_due_at %>.</p>
<% end %>

<p><%= t("devise.mailer.invitation_instructions.ignore").html_safe %></p>
